<div class="row justify-content-center my-5">
  <div class="col-12 col-md-9 col-lg-6 bg-secondary-subtle p-4">
    <form class="d-flex" style="flex-direction: column; gap: 1rem" action="/conta/alterar-senha-confirm" method="POST">
      <h4>Alterar senha</h4>
      <p>Foi enviado um email contendo um código de confirmação para:</p>
      {{email}}
      <div>
        <label class="form-label" for="authCode">Insira o código enviado:</label>
        <input class="form-control" name="authCode" id="authCode" required>
      </div>
      <div>
        <input class="btn btn-success" type="submit" value="Alterar senha">
      </div>
      <div class="text-danger">
        {{message}}
      </div>
    </form>
  </div>
</div>